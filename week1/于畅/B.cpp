#include <bits/stdc++.h>
using namespace std;

#define INF 0x3f3f3f3f
typedef long long LL;

int main()
{
    /*vector<pair<int,int>> point;
    set<pair<double,double>> str;

    for(int i=0;i<20;i++)
    for(int j=0;j<21;j++)
        point.push_back(make_pair(i,j));

    for(int a=0;a<point.size();a++)
    for(int b=a+1;b<point.size();b++){
        if(point[a].first==point[b].first||point[a].second==point[b].second) continue;
        double k=(point[b].second-point[a].second)*1.0/(point[b].first-point[a].first);
        //double b1=point[a].second-k*point[a].first;
        double b1=(point[a].second*point[b].first-point[a].first*point[b].second)*1.0/(point[b].first-point[a].first);
        str.insert(make_pair(k,b1));
    }

    int ans=str.size()+20+21;

    cout<<ans;*/

    cout<<40257;

    return 0;
}
