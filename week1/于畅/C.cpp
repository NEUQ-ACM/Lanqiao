#include <bits/stdc++.h>
using namespace std;

typedef long long LL;
const LL n = 2021041820210418;

int main()
{
    /*vector<LL> ys;

    for(int i=1;i<=sqrt(n);i++){
        if(n%i==0){
            ys.push_back(i);
            if(i!=n/i) ys.push_back(n/i);
        }
    }

    int count=0;

    for(LL i=0;i<ys.size();i++){
        for(LL j=0;j<ys.size();j++){
            LL k=n/(ys[i]*ys[j]);
            if(ys[i]*ys[j]*k==n) count++;
        }
    }

    cout<<count;*/
    
    cout<<2430;

    return 0;
}
